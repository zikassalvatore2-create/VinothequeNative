name: Build Native Android

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.5'
        
    - name: Build with Gradle
      run: gradle assembleDebug --stacktrace --info 2>&1 | tee build.log; exit ${PIPESTATUS[0]}
      continue-on-error: true
        
    - name: Show Kotlin errors
      if: always()
      run: |
        echo "=== KOTLIN COMPILER ERRORS ==="
        grep -n "^e:" build.log || echo "No e: lines found"
        echo ""
        echo "=== LAST 100 LINES ==="
        tail -100 build.log
        
    - name: Upload APK
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
